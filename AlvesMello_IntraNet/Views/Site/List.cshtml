@model SiteListViewModel

<div>
	<h2>@Model.CurrentCategory</h2>
</div>

<div class="container">
	<div class="row">
		@foreach (var site in Model.Sites)
		{
			<partial name="_SitesSummary" for="@site" />
		}
	</div>
</div>

